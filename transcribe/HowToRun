# Create virtualenv
virtualenv env

source env/bin/activate 

# Install required packages
pip install -r requirements.txt

#Export key
export GOOGLE_APPLICATION_CREDENTIALS="/Users/krishnak/Work/Leizure/elx-wkshp/python-docs-samples/speech/cloud-client/My First Project-302a7f10fd92.json"

erlc transcribe.erl

erl -name speech_server@127.0.0.1

#Commands to be run inside erl shell
net_kernel:connect('phoenix_speak_up@127.0.0.1').
global:registered_names().
{ok,P}=python:start().
python:call(P,transcribe_streaming_mic,main,[]).
